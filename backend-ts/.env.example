# Environment
NODE_ENV=development
BACKEND_PORT=8080

# Canton/Ledger
LEDGER_HOST=localhost
LEDGER_PORT=3901
APP_PROVIDER_PARTY=

# Database (PQS)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=pqs-app-provider
POSTGRES_USERNAME=cnadmin
POSTGRES_PASSWORD=supersafe

# Auth Mode (oauth2 or shared-secret)
AUTH_MODE=oauth2
AUTH_APP_PROVIDER_BACKEND_USER_NAME=app-provider-backend

# OAuth2 (when AUTH_MODE=oauth2)
OAUTH2_ISSUER_URL=http://keycloak.localhost:8024/realms/canton
OAUTH2_CLIENT_ID=app-provider-backend
OAUTH2_CLIENT_SECRET=

# Shared Secret (when AUTH_MODE=shared-secret)
SHARED_SECRET=

# Validator
VALIDATOR_URI=http://localhost:5003

# Logging
LOG_LEVEL=info
